# 让 Git 自动规范化文本文件行尾
# 提交进仓库统一 LF；签出到工作区按平台约定（Windows→CRLF，其它→LF）
* text=auto eol=lf

# 明确标记为二进制，避免任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.pdf binary
*.ico binary
*.woff binary
*.woff2 binary
